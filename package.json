{
  "name": "design-system-stencil",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev --parallel --filter=./packages/*",
    "storybook": "turbo run dev storybook --parallel --filter=@lm-prototype-stencil/components...",
    "dev:react": "turbo run dev --parallel --filter=@lm-prototype-stencil/test-react...",
    "dev:angular": "turbo run dev --parallel --filter=@lm-prototype-stencil/test-angular...",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "turbo": "^2.9.6",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0"
}